<div class="container mt-4">
    <div class="row">
        <div class="col-lg-9">

                <div class="row container-detail">
                    <div class="col-lg-3 image-container">
                        <img src="{{movies.image}}" alt="">
                    </div>
                    <div class="col-lg-9 container-desc">
                        <h5>{{movies.name}}</h5>
                        <p>{{movies.title}}</p>
                        <span>{{movies.releaseDate}}</span>
                        <p class="espisode">{{movies.espisode}}</p>
                        <div class="test-sm">
                            <span>The loai:{{movies.category}}</span>
                            <span>{{movies.tag}}</span>
                        </div>
                        <div class="container-btn">
                            <button type="button" class="btn btn-danger">Xem Ngay</button>
                            <button type="button" class="btn btn-secondary">Follow</button>
                        </div>
                    </div>
                </div>

        </div>
        <div class="col-lg-3">
            top xem nhiu
        </div>
    </div>
</div>